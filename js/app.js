/**
 * APPLICATION PRINCIPALE — TOM SAX
 * Orchestration globale, chargement des composants, navigation mobile.
 */

document.addEventListener("DOMContentLoaded", () => {
  // Initialisation globale de l'application
  console.log("Tom Sax Website - Initialisé avec succès");
});
