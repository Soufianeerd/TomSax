/**
 * GESTION DU FORMULAIRE DE CONTACT & DEVIS — TOM SAX
 * Validation côté client, accessibilité des champs, gestion des états soumission.
 */

const FormModule = {
  init() {
    // Initialisation ultérieure du formulaire de devis qualifié
  }
};

document.addEventListener("DOMContentLoaded", () => {
  FormModule.init();
});
