/**
 * MODULE MUSIQUE & RÉPERTOIRE — TOM SAX
 * Gestion du filtre des morceaux, recherche, lecture d'extraits.
 * Sera alimenté suite à la fourniture et lecture du fichier de répertoire musical.
 */

const MusicModule = {
  init() {
    // Initialisation ultérieure avec les données réelles
  }
};

document.addEventListener("DOMContentLoaded", () => {
  MusicModule.init();
});
